[package]
name = "sour"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "A customizable brainfuck interpreter."

[dependencies]
clap = { version = "4.1.1", features = ["derive"] }

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
